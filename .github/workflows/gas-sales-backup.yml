name: Deploy GAS (売上バックアップ)

on:
  push:
    branches: [main]
    paths:
      - 'sawasdee-pos/apps_script/sales-backup/**'
      - '.github/workflows/gas-sales-backup.yml'
  workflow_dispatch: {}

concurrency:
  group: gas-sales-backup
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: sawasdee-pos/apps_script/sales-backup
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install clasp
        run: npm install -g @google/clasp@3.3.0

      - name: Write clasp credentials
        env:
          CLASPRC: ${{ secrets.CLASPRC_JSON }}
        run: |
          if [ -z "$CLASPRC" ]; then
            echo "::error::CLASPRC_JSON secret is empty. Register it under repo Settings → Secrets."
            exit 1
          fi
          printf '%s' "$CLASPRC" > ~/.clasprc.json
          chmod 600 ~/.clasprc.json

      - name: Push to Apps Script
        run: clasp push -f

      - name: Deploy (update existing deployment, keep same URL)
        run: |
          STAMP=$(date -u +%Y-%m-%dT%H:%MZ)
          clasp deploy \
            --deploymentId AKfycbw3x1I7GyV2WrisvKZBHpyapfdXf6blir4v00Lljfa_iin4OaYuNc2CiaN8RO_bmitD \
            --description "GitHub Actions $STAMP"
